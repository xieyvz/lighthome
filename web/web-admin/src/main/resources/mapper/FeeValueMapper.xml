<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xieyv.lighthome.web.admin.mapper.FeeValueMapper">

    <select id="selectListByCommunityId" resultType="com.xieyv.lighthome.web.admin.vo.fee.FeeValueVo">
        select v.id,
               v.name,
               v.unit,
               v.fee_key_id as feeKeyId,
               k.id,
               k.name as feeKeyName
        from fee_key as k
                 JOIN fee_value as v
                      on k.id=v.fee_key_id
        where k.is_deleted=0
          and v.is_deleted=0
          and v.id in
              (select fee_value_id
               from community_fee_value
               where is_deleted=0
                 and community_id=#{id})
    </select>
</mapper>
