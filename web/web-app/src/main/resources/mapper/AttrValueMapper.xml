<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xieyv.lighthome.web.app.mapper.AttrValueMapper">

    <select id="selectVoListByRoomId" resultType="com.xieyv.lighthome.web.app.vo.attr.AttrValueVo">
        select v.id,
               v.name,
               v.attr_key_id,
               k.name as attr_key_name
        from attr_key as k
                 left join attr_value as v
                           on k.id=v.attr_key_id and v.is_deleted=0
        where k.is_deleted=0
          and v.id in (select attr_value_id
                       from room_attr_value
                       where is_deleted=0
                        and room_id=#{id})
    </select>
</mapper>
