<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xieyv.lighthome.web.app.mapper.ViewAppointmentMapper">

    <select id="selectListItem" resultType="com.xieyv.lighthome.model.entity.ViewAppointment">
        select
            va.id,
            ci.name,
            va.community_id,
            va.appointment_time,
            va.appointment_status
        from view_appointment as va
                 left join community_info as ci
                           on va.community_id=ci.id and ci.is_deleted=0
        where va.is_deleted=0
    </select>
</mapper>
